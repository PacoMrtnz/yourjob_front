export interface Jobs {
    title: string,
    slug: string,
    categoria: string,
    description: string,
    location: string,
    salario?: number,
    duracion?: string,
    horas?: number,
    author?: string,
    createdAt?: string,
    updatedAt?: string,
    _id?: string
}